# KAIZEN — Nhật ký lỗi & cải tiến

Mục đích: mọi bug phát hiện được (do user, gate, hay QC) PHẢI ghi vào đây để không lặp lại. Quy tắc: mô tả ngắn — gốc rễ — cách phòng ngừa đã đưa vào hệ thống (rule/gate nào). Bug chưa có phòng ngừa tự động = việc còn nợ.

**Template entry:**

```
## [YYYY-MM-DD] Tên bug ngắn gọn
- **Hiện tượng:** ...
- **Gốc rễ:** ...
- **Phòng ngừa:** Rule X.Y + gate `lệnh` (hoặc "CHƯA CÓ — cần bổ sung")
```

---

## [2026-07-22] Bước 3 tạo FULL sfx cho 186/186 scene — sfx phải là điểm nhấn cảm xúc, không phải lớp phủ
- **Hiện tượng:** user báo khi chạy Bước 3 (model Opus), sfx được tạo cho TOÀN BỘ scene của video — 186/186 entry đều có keyword, kể cả scene tĩnh chỉ nhận room tone chung chung. Mục đích thật: sfx đặt đúng vị trí để tăng cảm xúc (có gió thì tiếng gió, có chuông thì tiếng chuông) với tỉ lệ vừa phải — video kể chuyện đã có narration + nhạc nền phủ suốt.
- **Gốc rễ:** (1) Rule 8.2 cũ ghi "scene tĩnh trong phòng → dùng room tone" — đọc như chỉ dẫn MẶC ĐỊNH, nên model làm kỹ sẽ lấp đầy mọi scene (chính phiên Fable 5 trước đó cũng ra 186/186 và còn báo cáo "0 scene im lặng" như thành tích). (2) Rule 8.4 coverage ("mọi scene phải có entry") bị hiểu nhầm thành "mọi scene nên có keyword". (3) Không có ràng buộc tỉ lệ, gate không đo mật độ → full-coverage vẫn PASS.
- **Phòng ngừa:** (1) Rule 8.9 mới — sfx là ĐIỂM NHẤN: tỉ lệ kỳ vọng ~15-40% scene, 3 tiêu chí chọn (sự kiện âm narration nhắc tới / đỉnh cảm xúc / motif lặp); 8.2 sửa lại: `[]` là mặc định, room tone chỉ khi cảnh cần nền đặc trưng. (2) Gate `--step3` đo mật độ: >75% scene có keyword = LỖI FULL-COVERAGE, >50% = cảnh báo. Self-test trên data thật: 186/186 → FAIL đúng. (3) Skill `step3-sound` thêm MỤC TIÊU SỐ (in khoảng kỳ vọng trước khi viết) + thuật toán 2 lượt (chọn điểm nhấn trước, viết keyword sau). (4) Vá data DLS_31: trim 186→68 scene điểm nhấn (37%, giữ trọn motif chuông đồng 10 scene), rebuild sfx.json + sfx-timeline.json + metadata.json → mọi gate PASS.

## [2026-07-22] Nhân vật chỉ nói qua ĐIỆN THOẠI bị liệt kê ở cột Characters → prompt buộc gọi tên người không có trong khung
- **Hiện tượng:** project DLS_31 scene 49 (Corinne nghe Burl nói qua điện thoại) có cột Characters ghi `burl, corinne`. Sub-agent Bước 4 phải thỏa character lock QC1 (mọi nano_name trong cột Characters PHẢI xuất hiện trong prompt) nên viết `Corinne ... while speaking with Burl on the line` — Burl không hề có mặt trong khung hình. Mọi gate `--step2/--step4` đều PASS.
- **Gốc rễ:** cột Characters ở Bước 2 bị hiểu là "nhân vật tham gia phân cảnh" (kể cả qua giọng nói) thay vì "nhân vật NHÌN THẤY ĐƯỢC trong ảnh". QC1 lại là ràng buộc cứng theo cột đó, nên một mục thừa ở Bước 2 ép prompt phải gọi tên → model tạo ảnh nhiều khả năng vẽ thêm một người thứ hai vào cảnh vốn chỉ có một người cầm ống nghe. Cùng họ với Rule 4.6 (visual state, not narrative): thứ không nhìn thấy được thì không thuộc về prompt ảnh.
- **Hệ quả nếu lọt:** ảnh sai nội dung (2 người trong cảnh gọi điện một mình), và tệ hơn là sai ở cảnh có ý nghĩa kịch bản — khoảng cách vật lý giữa hai nhân vật là chi tiết kể chuyện.
- **Phòng ngừa:** (1) Gate tự động — `gate_step2()` thêm cảnh báo: scene có ≥2 nhân vật MÀ description chứa dấu hiệu cuộc gọi (`qua/trên/bằng điện thoại`, `gọi điện`, `nhấc máy`, `đầu dây`, `on the phone`) → nhắc bỏ người ở đầu dây bên kia khỏi cột Characters. Đã self-test: trả lại `burl` vào scene 49 → BẮT ĐƯỢC, bỏ ra → sạch. (2) Skill `step2-beat` + `step4-prompts`: cột Characters chỉ gồm nhân vật nhìn thấy được trong khung. (3) Đã vá data: scene 49 bỏ `burl`, prompt 49 viết lại thành Corinne nghe điện thoại một mình; chạy lại `--step2` + `--step4` → PASS.

## [2026-07-22] Âm đánh chữ tràn toàn video khi dựng: sai thời gian với chữ, vang cả đoạn không chữ, sfx đơn điệu
- **Hiện tượng:** user dựng video: (1) âm gõ phím lệch thời gian với chữ đang gõ (chữ 1 nơi, âm 1 nơi); (2) đoạn không có chữ vẫn có âm đánh chữ; (3) toàn bộ lớp sfx chỉ là âm đánh chữ, không âm bối cảnh nào khác; (4) user yêu cầu thêm: sfx phải hợp mood + đa dạng.
- **Gốc rễ:** hợp đồng giao editor không đủ chặt để chặn hiểu nhầm, và không gate nào kiểm nội dung sfx: (1) `typewriter-cues.md` chỉ có bảng cue, KHÔNG nói âm gõ phím đặt ở đâu/kéo dài bao lâu/chỗ nào cấm — editor tự suy → rải âm gõ theo scene thay vì theo cue; (2) không có thời lượng gõ nên điểm DỪNG âm không xác định → lệch với chữ; (3) `gate_step3` chỉ kiểm coverage/schema, KHÔNG kiểm nội dung keyword — nếu Bước 3 đổ `typewriter keys` vào mọi scene, gate vẫn PASS (cùng họ lỗi "cảnh báo ≠ chặn" với entry granularity cùng ngày); (4) cue `at` ngoài khoảng scene chỉ là cảnh báo — chữ hiện đè scene khác vẫn PASS; (5) không check đơn điệu — 1 keyword phủ cả video không lưới nào đo.
- **Phòng ngừa:** (1) **1 nguồn thời gian duy nhất cho âm đánh chữ**: `merge_typewriter` tính `type_s` = độ dài text × 0.18s (kẹp 1.5–6.0s) ghi vào metadata; `at` ngoài scene nâng cảnh báo → LỖI CỨNG; cảnh báo mới khi `at + type_s` vượt end scene (chữ chưa gõ xong đã sang cảnh). (2) **Hợp đồng editor tường minh**: `write_typewriter_cues` in khối "QUY TẮC ÂM THANH ĐÁNH CHỮ" đầu file (âm gõ CHỈ tại `Time`, dừng sau `Gõ (s)`; scene không cue = không âm đánh chữ; âm đánh chữ không lấy từ sfx) + cột `Gõ (s)`. (3) **Gate nội dung sfx** trong `gate_step3`: keyword dính `typewriter/typing/keyboard/keystroke` → LỖI trừ khi description scene có máy chữ/bàn phím thật (Rule 8.7 mới); 1 keyword phủ >50% scene có âm → LỖI, >25% → cảnh báo, lặp 4+ scene liên tiếp → cảnh báo (Rule 8.8 mới — đa dạng + bảng chọn âm theo mood). (4) Checklist QC định tính thêm mục SFX (mood-fit máy không kiểm được). (5) Skill `step3-sound`/`step5-export` cập nhật. Self-test: giả lập cả 3 lỗi → gate bắt đúng; data project hiện tại (176 scene, 12 cue) PASS + regenerate cues/metadata theo format mới.

## [2026-07-22] Model yếu hơn cắt video 32 phút thành ~30 scene (có scene dài vài phút) — gate `--step2` vẫn PASS
- **Hiện tượng:** user chạy Bước 2 bằng model khác (Opus): video 32 phút chỉ ra hơn 30 phân cảnh, có phân cảnh kéo dài vài phút — trong khi Fable 5 cùng quy trình cắt đúng độ hạt (~10-14s/scene). Gate `--step2` không chặn được.
- **Gốc rễ:** (1) `gate_step2()` xếp duration ngoài khoảng Rule 3.1 là CẢNH BÁO, không phải lỗi — scene 300s vẫn in `✓ PASS`, model đọc dòng cuối tưởng đạt. (2) Không có check tổng quát nào về SỐ SCENE TỐI THIỂU theo độ dài video — cắt thô toàn cục không lưới nào đo. (3) Quy trình Bước 2 nằm rải trong CLAUDE.md dài — model yếu đọc lướt, không có "mục tiêu số" tính trước nên cắt theo cảm giác, quy trình phụ thuộc năng lực model thay vì bị máy ràng buộc.
- **Phòng ngừa:** (1) Gate tự động — `gate_step2()`: scene VƯỢT max của beat type → LỖI CỨNG (dưới min giữ cảnh báo); thêm check GRANULARITY: tổng scene < ⌈SRT/25⌉ (25s = max tuyệt đối mọi beat) → LỖI CỨNG kèm số kỳ vọng ~SRT/12. Self-test: giả lập 30 scene/1924s + scene 300s → bắt cả 2 loại; data project hiện tại vẫn PASS. (2) Tái cấu trúc quy trình thành SKILL đơn nhiệm: mỗi bước 1 skill trong `.claude/skills/` (step1-setup … step7-qc, pipeline), mỗi skill = Pre-flight → MỤC TIÊU SỐ tính trước (Bước 2: in số scene tối thiểu/kỳ vọng TRƯỚC khi cắt) → thuật toán → LOOP ENGINE (lặp gate đến PASS, ngắt an toàn sau 5 vòng, cấm nới check) → kết quả trả về. CLAUDE.md chuyển thành router bắt buộc mở skill khi chạy bước, không làm từ trí nhớ. (3) Data: output hiện tại thuộc project trước (đã đúng độ hạt) — không cần vá; bảng breakdown lỗi của lần chạy Opus không còn trong repo.

## [2026-07-21] Visual Notes Bước 2 ghi lens sai quy ước → cả 176 prompt Bước 4 thiếu chữ `lens`, gate chỉ cảnh báo
- **Hiện tượng:** `--step4` PASS nhưng in 176 cảnh báo "Prompt N: shot type chưa kèm lens (rule 4.8)" — đúng bằng tổng số scene, tức 100% prompt dính. Prompt thực tế có tiêu cự (`extreme wide shot 24mm`, `close-up 85mm portrait`) nên nhìn bằng mắt tưởng đã đủ lens.
- **Gốc rễ:** ở Bước 2 tôi soạn cột Visual Notes bằng ký pháp tự nghĩ ra (`shot type` + `<số>mm`) thay vì copy đúng tên lens trong bảng Rule 4.8 (`24mm wide-angle lens`, `35mm lens`, `50mm lens`, `85mm lens`, `85mm portrait lens`, `100mm macro lens`). Spec sub-agent Bước 4 lại yêu cầu "COPY NGUYÊN VĂN shot type từ Visual Notes" (để khỏi phá lưới shot distribution) — nên 6/6 agent nhân bản trung thành đúng cái sai của nguồn. Lỗi 1 lần ở Bước 2 → lan 176 lần ở Bước 4. `gate_step2()` không kiểm Visual Notes nên không chặn tại nguồn; `gate_step4()` chỉ hạ xuống mức CẢNH BÁO nên vẫn in PASS — dễ bị bỏ qua nếu chỉ nhìn dòng cuối.
- **Hệ quả nếu lọt:** mất tên lens tường minh — thứ Rule 4.8 nói là làm ảnh ổn định hơn hẳn; đồng thời mất tính nhất quán ống kính giữa các scene cùng loại shot.
- **Phòng ngừa:** (1) Gate tự động — thêm check vào `gate_step2()`: mọi ô Visual Notes có cụm shot (`shot|close-up`) PHẢI chứa chữ `lens`, thiếu = LỖI cứng ngay Bước 2, chặn trước khi sinh prompt. (2) Rule 3.x (beat-analysis.md) siết: Visual Notes phải ghi shot type kèm ĐÚNG tên lens theo bảng Rule 4.8, không tự đặt ký pháp `<số>mm` rời. (3) Đã vá data: map lại 9 ký pháp cũ sang tên lens chuẩn trên cả `scene-breakdown.md` (nguồn), `prompts.md` và `video-prompts.md`; chạy lại `--step2` + `--step4` → PASS 0 cảnh báo, không prompt nào vượt 75 từ.

## [2026-07-20] mood_map chứa cụm SHOT SCALE → prompt mang 2 chỉ dẫn khung hình chọi nhau, mọi gate đều PASS
- **Hiện tượng:** QC định tính Bước 7 phát hiện 9/22 scene mood `shock` có Phần 3 (shot type) mâu thuẫn với Phần 4 (mood keywords). Rõ nhất: scene 69 là `extreme wide shot, 24mm wide-angle lens` (phòng xử án trống) nhưng prompt vẫn kèm `extreme close-up` do mood_map[shock] = "dramatic side lighting, shallow DOF, extreme close-up". Tương tự scene 36/44/86/145/151 (medium), 76/141 (angle), 144 (wide). Toàn bộ `--step1/2/3/4` và `--qc` đều PASS 0 lỗi 0 cảnh báo — không lưới nào bắt.
- **Gốc rễ:** bảng `mood_map` mặc định trong CLAUDE.md (Bước 1) và visual-style.md 2.2 gán mood `shock` = "dramatic side lighting, shallow DOF, **extreme close-up**" — một cụm SHOT SCALE nằm lẫn trong bộ keyword lẽ ra chỉ gồm ánh sáng + màu + khung hình (Rule 4.7). Mood được append vào MỌI prompt mang mood đó, nên cụm scale này bám cả những scene có shot hoàn toàn khác. Không gate nào so khớp chéo Phần 3 với Phần 4 vì cả hai đều "đúng nguyên văn nguồn của mình" — lỗi nằm ở chính NGUỒN (config), không phải ở khâu ghép.
- **Hệ quả nếu lọt:** model tạo ảnh nhận 2 lệnh framing ngược nhau, thường ưu tiên cụm mạnh hơn (`extreme close-up`) → những scene wide/establishing sẽ ra ảnh cận, phá vỡ shot distribution Rule 4.2 ở tầng ẢNH THẬT dù bảng prompt vẫn "đạt quota".
- **Phòng ngừa:** (1) Gate tự động — thêm check vào `gate_step1()`: mọi giá trị trong `mood_map` bị quét regex cụm shot scale (`extreme close-up|close-up|extreme wide|wide shot|medium shot|over-shoulder|low angle|high angle|macro|\d+mm`), dính = LỖI cứng ngay Bước 1, trước khi sinh bất kỳ prompt nào. Đã self-test: config sạch PASS, cố tình trả lại cụm cũ → BẮT ĐƯỢC. (2) Rule 2.2 (visual-style.md) + Rule 4.7 (prompt-structure.md) siết: mood_map CHỈ gồm ánh sáng/màu/khung hình, CẤM cụm shot scale — scale thuộc Phần 3 và do Bước 2 quyết theo quota Rule 4.2. (3) Đã vá data: `mood_map[shock]` → "dramatic side lighting, shallow depth of field, stark tonal contrast"; build lại 187 prompt, chạy lại toàn bộ gate + Bước 5 → PASS.

## [2026-07-17] Sub-agent Bước 4 bỏ dấu phẩy trong cụm mood keywords → --step4 PASS nhưng --qc QC2 FAIL 60 scene
- **Hiện tượng:** 6 sub-agent batch bội-số-10 sinh prompts, `--step4` PASS. Nhưng `--qc` QC2 báo 60 lỗi "thiếu mood keywords nguyên văn" — đúng 2 batch (61-90 và 121-150) × 30 scene. Ví dụ scene 62 viết `strong directional light firm posture clear focus` thay vì `strong directional light, firm posture, clear focus` (bỏ dấu phẩy nội bộ). 4 batch còn lại giữ dấu phẩy nên không lỗi.
- **Gốc rễ:** (1) chỉ đạo sub-agent nói "copy NGUYÊN VĂN mood keywords" nhưng một số agent tự chuẩn hóa bỏ dấu phẩy khi ghép vào câu prompt (coi phẩy là thừa) — hành vi không nhất quán giữa các agent. (2) `gate_step3()` CŨ chỉ kiểm style anchor verbatim, KHÔNG kiểm mood keywords verbatim; check mood chỉ nằm ở `--qc` QC2 → lỗi lọt qua Bước 4, tới tận Bước 7 mới lộ, tốn thêm 1 vòng gate.
- **Phòng ngừa:** (1) Gate tự động — thêm check mood verbatim vào `gate_step3()` (mirror QC2): mỗi prompt phải chứa nguyên văn `mood_map[scene.mood]`, thiếu = LỖI cứng ngay Bước 4. Đã self-test: bản sạch PASS, cố tình bỏ 1 phẩy → BẮT ĐƯỢC. (2) Rule 4.1 Phần 4 (prompt-structure.md) siết: mood keywords copy nguyên văn KỂ CẢ dấu phẩy nội bộ — CẤM bỏ/đổi phẩy; nhắc rõ trong spec sub-agent. Đã vá data: replace 60 cụm mood commaless→có phẩy trong prompts.md, chạy lại Bước 5 + `--qc` → PASS 0 lỗi.

## [2026-07-15] Batch bội-số-10 vẫn thiếu close-up mỗi thập kỷ vì "medium close-up" bị shot_cat xếp nhóm MEDIUM
- **Hiện tượng:** Bước 4 chạy 6 sub-agent batch bội-số-10 (1-30, 31-60...) đúng theo kaizen 2026-07-11, self-check "shot variety OK", `--step4` PASS. Nhưng `--qc` QC5 báo 19 cảnh báo Rule 4.2: 9 thập kỷ thiếu closeup (nhiều decade 0/2), vài chỗ 4 medium liên tiếp (scene 65,79,93,105,121,143), 71-80 thiếu wide, 81-90 & 151-160 thiếu angle.
- **Gốc rễ:** `shot_cat()` phân loại theo LENS — `85mm lens` (medium close-up) và `50mm lens` (medium shot) đều rơi vào nhóm **medium**; chỉ `85mm portrait lens` (close-up) và `100mm macro` (extreme close-up) mới tính **closeup**. Visual Notes tôi soạn ở Bước 2 thiên `medium shot` + `medium close-up`, nên một thập kỷ nhìn "có vẻ đa dạng" (trộn medium shot + medium close-up + wide) vẫn đạt 0/2 closeup vì medium close-up KHÔNG đếm là closeup. Sub-agent bám Visual Notes nên nhân bản độ lệch. Đây KHÁC lỗi 2026-07-11 (lệch cửa sổ đếm) — lần này cửa sổ đã đúng, sai ở chỗ quota closeup cần close-up THẬT.
- **Phòng ngừa:** (1) Rule 4.2 / spec sub-agent Bước 4 phải ghi rõ: quota `closeup ≥2/thập kỷ` chỉ tính `close-up (85mm portrait)` + `extreme close-up (100mm macro)`; `medium close-up (85mm)` đếm là MEDIUM. Khi soạn Visual Notes Bước 2, mỗi 10 scene chủ động đặt ≥2 close-up thật ở beat mặt/cảm xúc/insert vật thể, ≥1 angle. (2) Gate tự động: `--qc` QC5 (đã có) là lưới bắt — lần này bắt đúng, chỉ tốn 1 vòng vá. Đã vá data: nâng 15 medium-close-up → close-up (85mm portrait) ở beat cảm xúc, 1 medium → wide (scene 76), 2 medium → low angle (84, 159); chạy lại Bước 5-5, `--qc` → 0 cảnh báo shot variety.

## [2026-07-11] Cặp scene cold-open/replay cùng một khoảnh khắc nhưng 2 prompt tả 2 căn phòng khác nhau
- **Hiện tượng:** QC định tính (sample khớp narration) phát hiện scene 71 (điện thoại rung lần thứ 11, úp máy xuống) là REPLAY của đúng khoảnh khắc scene 5 trong cold open, nhưng prompt 71 tả gian bán hàng trước (brass cash register) trong khi prompt 5 tả phòng sau (bồn rửa) — cùng một khoảnh khắc sẽ ra 2 không gian khác nhau khi vẽ.
- **Gốc rễ:** cấu trúc kể chuyện cold-open + "14 hours earlier" tạo ra các CẶP scene chiếu lại cùng khoảnh khắc ở 2 vị trí xa nhau trong bảng (5 vs 71, 1 vs 51). Sub-agent viết batch 3 (scene 61-90) không nhìn thấy prompt của scene 5 (batch 1) nên chọn bối cảnh theo suy diễn cục bộ; narration chỉ nói "the counter" — mơ hồ đủ để 2 batch chọn 2 phòng.
- **Phòng ngừa:** Rule 4.3 bổ sung: scene replay khoảnh khắc cold-open PHẢI dùng cùng setting với scene cold-open tương ứng; Bước 2 nên ghi chú "(replay scene N)" vào Visual Notes của các cặp này để Bước 4 (kể cả sub-agent) thấy ràng buộc ngay trong dòng scene. Gate tự động: CHƯA CÓ — máy khó nhận diện cặp replay; lưới hiện tại là bước sample QC định tính (chính là bước đã bắt được lỗi này). Đã vá: sửa setting prompt 71 về phòng sau (khớp scene 5), chạy lại Bước 5-5, --qc PASS.

## [2026-07-11] Veo 3 chặn prompt cold-open có thoại vì khung đe dọa + từ chỉ đạo tâm lý gây hấn
- **Hiện tượng:** user báo prompt Clip C1 trong `output/cold-open-veo.md` bị Veo 3 từ chối (vi phạm chính sách). Prompt chứa câu thoại "That's the only thing standing between you and losing everything your family built" (khung đe dọa mất mát cá nhân, kiểu uy hiếp tài chính) + các từ chỉ đạo giọng `warm-but-condescending`, `subtle contempt beneath politeness`; C3 có `mockingly`, `condescending` cùng loại.
- **Gốc rễ:** hồ sơ giọng trong narration.md viết cho TOOL VOICE riêng (chỉ đạo diễn xuất nội tâm), bị bê nguyên vào prompt Veo — bộ lọc của Veo đọc tổ hợp "lời thoại đe dọa + contempt/mock" như nội dung quấy rối/uy hiếp mô phỏng. Ý đồ phim (đe dọa bọc đường) không cần nằm trong TỪ chỉ đạo — nó nằm ở hình ảnh (nụ cười cố định, ánh mắt phẳng, im lặng).
- **Phòng ngừa:** quy tắc soạn prompt Veo có thoại: (1) lời thoại tránh khung đe dọa cá nhân trực tiếp (mất mát, trừng phạt) — chuyển sang khung chào hàng/sự việc ("That well is the only thing that can keep this ranch alive"); (2) từ chỉ đạo giọng dùng trung tính (polished, self-assured, amused, soft and certain) — CẤM contempt/condescending/mocking/threatening trong prompt Veo; (3) subtext ác truyền qua mô tả THỊ GIÁC; (4) ghi chú cảnh báo ngay đầu cold-open-veo.md + fallback: bỏ quoted dialogue, generate cảnh lips-moving rồi lồng TTS riêng. Gate tự động: CHƯA CÓ — cold open ngoài pipeline, chưa đáng thêm gate riêng; nếu tái diễn ở project sau thì thêm wordlist check vào export.py.
- **Bổ sung cùng ngày (trigger thứ 2):** sau khi sửa từ vựng, Flow VẪN chặn — nghi phạm còn lại là mệnh đề `clean frame free of ... watermarks, logos ...` trong prompt video: bộ lọc Gemini/Flow đọc chữ "watermarks" thành yêu cầu GỠ watermark (loại request bị chặn cứng). Mệnh đề này giữ cho ảnh tĩnh Nano Banana/Uni-X (đã chạy ổn), nhưng với prompt VIDEO trong Flow thì bỏ hẳn — watermark Veo đóng sau render, prompt không gỡ được (xem entry 2026-07-09) nên mệnh đề vô dụng mà rủi ro cao. Đã gỡ khỏi 6 prompt cold open, thay bằng `clean uncluttered frame`. Thang xử lý 3 bậc ghi trong cold-open-veo.md.
- **Cập nhật 2026-07-20 (đã codify — tái diễn đúng như dự đoán):** project 29_COFFEE_CAN có video hook NẰM TRONG pipeline (`output/video-prompts.md`, 11 clip) nên rủi ro chặn không còn là chuyện "ngoài pipeline" nữa. Đã thực hiện đúng lời hẹn của entry này: (1) `config.video_hook.video_style` bỏ mệnh đề watermark → `clean uncluttered frame`; (2) `check_video_hook()` trong export.py đảo chiều — giờ chấp nhận `clean uncluttered frame`, và CẢNH BÁO nếu prompt video còn chứa `free of watermarks` (trước đây lại cảnh báo khi THIẾU nó, tức gate cũ đang đẩy người dùng vào đúng cái bẫy mà entry này mô tả). Lưu ý phạm vi: mệnh đề watermark VẪN GIỮ trong `config.style` cho ảnh tĩnh (Nano Banana/Uni-X chạy ổn, không bị lọc) — chỉ prompt VIDEO mới bỏ.

## [2026-07-11] Sub-agent Bước 4 tự kiểm shot distribution theo cửa sổ TƯƠNG ĐỐI batch → self-check PASS giả, --qc mới bắt
- **Hiện tượng:** 6 sub-agent song song sinh prompts (batch 27 scene: 1-27, 28-54, 55-81...) đều báo "shot distribution mỗi 10 scene OK", nhưng `--qc` QC5 báo 4 cửa sổ thiếu (61-70 thiếu angle, 131-140 thiếu wide, 141-150 & 151-160 thiếu closeup) + 1 scene thiếu nano_name (76, scene 2 nhân vật nhưng prompt chỉ gọi 1).
- **Gốc rễ:** gate kiểm cửa sổ theo THẬP KỶ TUYỆT ĐỐI (1-10, 11-20, ... 61-70), còn sub-agent tự kiểm theo cửa sổ tương đối với batch của mình (55-64, 65-74...) vì batch bắt đầu ở 55 — hai lưới lệch nhau nên window vắt qua ranh giới batch (61-70 thuộc 2 nhịp đếm khác nhau) không ai nhìn đúng. Batch size 27 (không phải bội số 10) khuếch đại độ lệch. Đây là biến thể của bài học 2026-07-08 (ràng buộc toàn cục vs batch cục bộ) — lần này gate máy đã có sẵn nên bắt được ngay, chỉ tốn 1 vòng sửa.
- **Phòng ngừa:** gate `--qc` QC5 (đã có) là lưới cuối. Giảm số vòng sửa: CLAUDE.md Bước 4 bổ sung — batch sub-agent nên chia theo BỘI SỐ 10 (vd 30 scene: 1-30, 31-60...) và prompt cho sub-agent phải nói rõ cửa sổ kiểm là thập kỷ tuyệt đối theo STT toàn cục (61-70, 71-80...), không phải 10-scene đầu tiên của batch. Đã vá data: sửa shot 5 scene (64→over-shoulder, 136→wide, 145→close-up, 155→close-up, 76 thêm nano_name Dale) → `--qc` PASS 0 lỗi 0 cảnh báo.

## [2026-07-10] Uni-X đánh số prompt lại từ 1 mỗi file → mất mạch số khi tách nhiều trang
- **Hiện tượng:** user báo khi `--unix-export` tách >100 ảnh thành nhiều file, mỗi file đánh số `(Image Prompt i/n)` LẠI TỪ ĐẦU với mẫu số là số ảnh của riêng file đó (vd file 1: `1/100…100/100`, file 2: `1/7…7/7`). User muốn số LIÊN TỤC toàn cục và mẫu số là TỔNG THỰC: file 1 `1/107…100/107`, file 2 `101/107…107/107` — để biết đang ở ảnh thứ mấy trên tổng bộ khi chạy nhiều lượt vẽ.
- **Gốc rễ:** trong `export_unix()`, vòng lặp ghi header dùng `enumerate(chunk, start=1)` (chỉ số cục bộ trong chunk) và `n = len(chunk)` (số ảnh của file hiện tại) → cả tử số lẫn mẫu số đều theo từng file, không theo toàn bộ.
- **Phòng ngừa:** sửa `export_unix()` — thêm `total = len(rows)` (tổng toàn bộ), đánh chỉ số toàn cục `gidx = b*UNIX_BATCH_SIZE + j + 1`, ghi `(Image Prompt {gidx}/{total})`. Đã vá dữ liệu dự án hiện tại (chạy lại `--unix-export`: file 1 `1/107→100/107`, file 2 `101/107→107/107`). Không cần gate riêng (chỉ là định dạng hiển thị; sai lệch nhìn thấy ngay trong file).

## [2026-07-09] Bước 5 âm thầm bỏ qua typewriter khi thiếu input/typewriter.json → metadata ra đời không có hiệu ứng đánh máy
- **Hiện tượng:** user báo `metadata.json` không có field `typewriter` nào. Gốc: khi thay SRT sang dự án mới (Dead Ponds), `input/typewriter.json` của dự án cũ bị xóa và KHÔNG được tạo lại cho dự án mới; Bước 5 chạy `export.py`, `load_typewriter()` trả `None`, nhánh `if tw:` bị bỏ qua HOÀN TOÀN im lặng — không log, không cảnh báo, không đề xuất. Video kể chuyện 65+ (có sẵn nhiều mốc mạnh: "$3,800", "2 năm sau", "~1,800 con cá chết", note của cha, "six figures", moral kết) ra đời mất trắng lớp text overlay mà không ai nhận ra tới tận review.
- **Gốc rễ:** typewriter được thiết kế "tùy chọn" nên khi vắng file nguồn, cả tầng máy (export.py) lẫn tầng người (CLAUDE.md Bước 5) đều KHÔNG có bước surface/đề xuất — "optional" bị hiểu thành "im lặng skip". Không có tín hiệu nào phân biệt "chủ ý không dùng typewriter" với "quên tạo file cho dự án mới".
- **Phòng ngừa:** (1) `export.py` — thêm nhánh `else` in **LƯU Ý** rõ ràng khi thiếu `input/typewriter.json` (không phải lỗi cứng vì typewriter vốn optional), gợi ý các mốc nên đặt cue + cách tạo file rồi chạy lại. (2) CLAUDE.md Bước 5 — quy định AI KHÔNG được im lặng đi tiếp: sau khi Bước 5 PASS mà thiếu file, AI phải chủ động ĐỀ XUẤT bộ cue (con số/time card/thesis/moral) cho user duyệt rồi tạo, TRỪ KHI user nói rõ không dùng. (3) Vá dữ liệu dự án hiện tại: tạo `input/typewriter.json` (9 cue thưa) → chạy lại export → metadata có `typewriter` + sinh `output/typewriter-cues.md`. Gate cứng cho việc "phải có typewriter": CỐ Ý KHÔNG thêm (typewriter thật sự optional, có video không cần) — dừng ở mức LƯU Ý + trách nhiệm AI đề xuất.

## [2026-07-09] Veo 3 tự vẽ icon AI / watermark vào nội dung video & ảnh
- **Hiện tượng:** user báo khi generate bằng Veo 3, video và ảnh bị "add icon AI" — xuất hiện icon/watermark kiểu AI nằm TRONG nội dung khung hình.
- **Gốc rễ:** prompt không có chỉ dẫn nào về khung hình sạch → model thỉnh thoảng tự vẽ watermark/logo/sparkle icon (học từ ảnh stock/ảnh AI có đóng dấu trong dữ liệu huấn luyện). Phân biệt 2 loại: (a) icon do MODEL VẼ vào nội dung — chặn được bằng prompt; (b) watermark Veo/Gemini do Google ĐÓNG lên video sau render (thường góc phải dưới) — prompt KHÔNG gỡ được, chỉ xử lý bằng tier trả phí cao hơn hoặc crop khi dựng.
- **Phòng ngừa (đã codify vào CORE):** mệnh đề `clean frame free of watermarks, logos, or AI icons` trở thành ĐUÔI BẮT BUỘC của mọi style string: (1) Rule 2.1a — 2 preset style kết bằng mệnh đề, style anchor copy nguyên văn vào mọi image prompt nên mọi ảnh tự có (gate style-verbatim sẵn có tự cưỡng chế); (2) CLAUDE.md Bước 1 + config.example.json — preset `style` và `video_hook.video_style` mặc định đã gồm mệnh đề; (3) Rule 6.3 — video prompt nhận mệnh đề qua `[video_style]`, gate `--step4` cảnh báo video prompt thiếu; (4) Rule 1.4 + 2.3a — template ảnh ref nhân vật/bối cảnh kết bằng mệnh đề; (5) Rule 4.4 + `export.py` — mệnh đề (8 từ) không tính vào ngân sách 20-75 từ; (6) Rule 4.6 — ghi rõ ngoại lệ "free of" (vẫn cấm no/not/without). Project hiện tại: đã áp cho video-prompts.md (11 clip) + `video_style` trong config; 170 image prompt đã sinh trong prompts.md GIỮ NGUYÊN theo quyết định user (ảnh đã/đang vẽ) — icon vẫn xuất hiện ở ảnh tĩnh thì vá style anchor + chạy lại từ Bước 4. Lưu ý: watermark Veo/Gemini do Google ĐÓNG sau render không gỡ được bằng prompt (tier trả phí/crop khi dựng).

## [2026-07-08] Gate shot_cat đếm sai nhóm shot: mood "extreme close-up" + cụm "establishing shot" làm QC5 báo nhầm
- **Hiện tượng:** sau khi gộp 7 batch Bước 4, kiểm phân bố shot (Rule 4.2) thấy `--qc` QC5b sẽ báo 16 cửa sổ thiếu — một phần do ĐO SAI: mọi scene mood `shock` (mood_map = "dramatic side lighting, shallow DOF, extreme close-up") bị `shot_cat` đếm là `closeup` dù shot thật là wide/medium; và prompt viết `extreme wide ESTABLISHING shot` trả `None` (7 scene) vì không khớp chuỗi "extreme wide shot".
- **Gốc rễ:** `shot_cat` dò theo CỤM SCALE ("extreme close-up", "wide shot"...) trên body CÓ CẢ mood keywords → cụm "extreme close-up" trong mood shock trùng cụm shot; và chèn "establishing" vào giữa làm vỡ khớp chuỗi.
- **Phòng ngừa:** viết lại `shot_cat` dò theo LENS trước (Rule 4.8: 24mm/`35mm lens`→wide, 50mm/`85mm lens`→medium, `85mm portrait`/`100mm macro`→closeup) + từ khóa góc máy (over-shoulder/low/high angle→angle); chỉ fallback cụm scale khi thiếu lens. Lens là tín hiệu deterministic, không trùng mood. Sau sửa: 0 scene `None`, đo đúng. Ngoài ra phân bố THẬT vẫn lệch (over-shoulder/angle ít, closeup nhiều) ở 16 cửa sổ → rebalance bằng solver. **BÀI HỌC (phát hiện muộn ở Bước 6):** solver ban đầu chỉ ràng buộc theo `beat` đã WIDEN nhầm ~11 scene "object-insert" (macro giấy tờ/tài liệu/bàn tay trên vật thể: sổ đỏ 1892, kính lúp trên chữ nhỏ, ký tắt mực xanh, sổ tay cân) thành wide/medium/angle → ảnh vô nghĩa (wide shot của chữ 8pt). Gốc: `beat` không phân biệt close-up VẬT THỂ với close-up MẶT NGƯỜI. Đã sửa solver: KHÓA (giữ close-up) cả hook 1-6 LẪN mọi object-insert (nhận diện: Part-1 của prompt KHÔNG mở đầu bằng nano_name → chủ thể là vật thể) + cấm widen emotional_peak sang wide; chỉ cho phép move an toàn (dialogue→over-shoulder, establishing/room→wide, mặt người→medium). Kết quả: 16→4 cửa sổ thiếu + 2 streak, đều nằm trong chuỗi đọc/khám phá hợp đồng (61-70 toàn macro tài liệu) — CHẤP NHẬN vì ép variety ở đó sẽ phá close-up vật thể. Bước 6 `--unix-export`: 50 scene không có setting tag đều hợp lệ (hook intro + close-up vật thể + 3 exterior toà nhà 1 lần).

## [2026-07-08] Gate --step2 không tự kiểm Rule 3.3 (chuyển beat cấm) & 3.6 (nghỉ mắt) → lỗi cấu trúc lọt khi gộp segment
- **Hiện tượng:** Bước 2 chia 5 sub-agent theo segment; gate `--step2` PASS nhưng khi rà tay toàn cục phát hiện 2 chuyển beat cấm (Rule 3.3): scene 124 `resolution`→`tension` ngay ranh giới seg3/seg4, scene 197 `establishing`→`emotional_peak`. Nếu không rà tay thì 2 lỗi cấu trúc này ship luôn.
- **Gốc rễ:** `gate_step2()` chỉ kiểm coverage/gap/Dur/beat hợp lệ/duration bounds/4+ streak — KHÔNG kiểm Rule 3.3 và 3.6. Hai rule này CLAUDE.md giao cho "rà tay ở phiên chính sau khi gộp batch" → phụ thuộc con người nhớ làm; sub-agent mỗi segment tự cân bằng cục bộ nên chuyển beat cấm dễ sinh ĐÚNG tại ranh giới giữa 2 segment (không segment nào nhìn thấy cả 2 phía).
- **Phòng ngừa:** thêm vào `gate_step2()`: (1) check Rule 3.3 — cặp `(prev_beat, cur_beat)` thuộc `FORBIDDEN_TRANS` {tension→resolution, establishing→emotional_peak, resolution→tension} → cảnh báo; (2) check Rule 3.6 — đếm chuỗi scene liên tiếp không thuộc `REST_BEATS` {establishing, reflection}, chạm 8 → cảnh báo. Đã vá data project này: scene 124→`establishing` (beat chuyển-bối-cảnh sang Des Moines), scene 197→`reflection`. 3 cảnh báo 3.6 còn lại (scene 45/111/156) là 3 cao trào cố ý dồn dập, đã ghi rõ lý do chấp nhận trong mục QC Notes của `scene-breakdown.md`.

## [2026-07-07] Video hook nhạt: 1 scene tách nhiều clip gần giống nhau (tua chậm cùng khung)
- **Hiện tượng:** user thấy trong hook "1 phân cảnh nhưng nhiều prompt không thay đổi nhiều" — mỗi scene bị tách .1/.2 chỉ khác camera motion (wide rồi slow push-in cùng khung), cảm xúc phẳng. User muốn mỗi clip là 1 cảnh khác hẳn, dồn cảm xúc mạnh trong ~1 phút.
- **Gốc rễ:** Rule 6.2 cũ cho phép clip .k "Extend từ clip trước" (tua tiếp cùng góc) làm mặc định → 2 clip cùng scene gần giống nhau; `duration_s` mặc định 120 (2 phút) làm hook loãng. Rule 6.6 điểm 2/6 lại nhấn "subject hiện diện suốt clip / Extend giữ nguyên mô tả" khiến hiểu nhầm là phải giữ cùng chủ thể xuyên các clip.
- **Phòng ngừa:** Rule 6.1 đổi `duration_s` mặc định 120→**60** (hook ~1 phút, cảm xúc mạnh dồn). Rule 6.2 viết lại: **mỗi clip = 1 CÚ CẮT (hard cut) sang shot/chủ thể/scale mới, có ảnh riêng** (clip .1 = ảnh scene; clip .k = ảnh phụ `[stt]-[k].jpg`); CẤM Extend tua chậm trong hook. Rule 6.6/6.7 làm rõ: subject persistence áp TRONG 1 clip, cú cắt GIỮA các clip là bình thường; escalation toàn hook như trailer. Gate `check_video_hook` sửa: chỉ ép subject-anchor ở clip .1 (clip .k hard-cut được cắt sang insert vật thể/bối cảnh không cần gọi nhân vật). CLAUDE.md Bước 4 cập nhật. Đã viết lại video-prompts.md project này: 12 clip hard-cut, 0→1:08.

## [2026-07-07] Ảnh ingredient nhân vật bị vẽ SAI GIỚI TÍNH (Wade nam → ra nữ)
- **Hiện tượng:** user vẽ ảnh ingredient thấy Wade (thợ máy 25 tuổi) ra con gái, còn Young Wade (11-12) ra con trai đúng.
- **Gốc rễ:** `characters[].description` của Wade là `"25-year-old diesel mechanic, ...easy warm smile..."` — KHÔNG có danh từ giới tính tường minh (man/woman); nghề "diesel mechanic" + "warm smile" trung tính nên model image gen tự đoán → đoán ra nữ. Young Wade có chữ "boy" nên đúng. Rule 1.1/1.1b trước chỉ ép màu trang phục, không ép giới tính. Sai ở ảnh ingredient lan ra mọi scene tham chiếu.
- **Phòng ngừa:** Rule 1.1c mới — `description` PHẢI mở đầu bằng `[tuổi]-year-old man/woman/boy/girl` trước nghề nghiệp/đặc điểm; cấm dựa vào tín hiệu gián tiếp (nghề, stubble, đại từ ở field name). Gate `--step2` thêm check cảnh báo character thiếu token giới tính (`man/woman/boy/girl/male/female`). CLAUDE.md Bước 1 ghi rõ. Đã vá config project này: silas, silas_young, wade thêm danh từ giới tính + regenerate 3 ảnh ref tương ứng.

## [2026-07-07] Setting là asset trong Uni-X nhưng KHÔNG có ảnh ingredient để vẽ
- **Hiện tượng:** prompt Uni-X có tag `[COURTROOM]` (và các setting khác) nhưng `character-refs.md` chỉ sinh ảnh ref cho nhân vật → user không có ảnh bối cảnh COURTROOM nào để upload làm ingredient.
- **Gốc rễ:** `--unix-export` gắn asset tag cho CẢ nhân vật lẫn bối cảnh (setting `id` viết hoa), coi setting là ingredient reference; nhưng Bước 1 (Rule 1.4) chỉ sinh ref prompt cho nhân vật. Thiếu bước sinh ref cho setting.
- **Phòng ngừa:** Rule 2.3a mới — Bước 1 sinh ảnh ref cho TỪNG setting (template ảnh nền trống, không người, medium trung tính) vào chung `character-refs.md` mục "Bối cảnh / Setting Assets"; CLAUDE.md Bước 1 step 6 + sơ đồ cấu trúc cập nhật. Đã bổ sung 13 setting ref vào character-refs.md của project này.

## [2026-07-07] Uni-X mất tag bối cảnh do khớp case-sensitive + cụm định danh quá dài
- **Hiện tượng:** `--unix-export` báo 45 scene không xác định setting; trong đó scene 3, 66 (toàn/cực rộng bức tường rác) bị trượt dù prompt chứa ĐÚNG cụm định danh — chỉ khác ở chữ đầu VIẾT HOA ("Long wall..." vì cụm đứng đầu prompt Phần 2); scene 30, 188 trượt vì cụm định danh `east_fence_wall` dài ("...six feet high along a wire fence line") nên prompt viết bản rút gọn không khớp verbatim.
- **Gốc rễ:** (1) `find_setting_id` khớp `probe in prompt_text` phân biệt hoa-thường → cụm đứng đầu prompt (viết hoa) không khớp keyword (viết thường). (2) Cụm định danh đầu tiên của `settings[].keywords` quá dài, agent hay lược bớt phần đuôi → không còn khớp verbatim. Cùng họ lỗi với KAIZEN 2026-07-06 (cụm định danh phải ngắn + ổn định).
- **Phòng ngừa:** `export.py` sửa `find_setting_id` + chèn tag sang khớp KHÔNG phân biệt hoa-thường (`re.IGNORECASE`); rút gọn cụm định danh `east_fence_wall` để cụm đầu (trước dấu phẩy) là chuỗi ngắn ổn định "long wall of dark black-brown slum gum residue" (phần "six feet high..." chuyển ra sau phẩy). Quy ước bổ sung cho Rule 4.1 Phần 2: **cụm định danh nên NGẮN** (một danh ngữ lõi), tránh nhồi kích thước/vị trí vào cụm đầu. Sau vá: 45→40 scene, 40 còn lại đều là cận cảnh vật thể / không gian phụ 1 lần (chấp nhận).

## [2026-07-01] Tràn context bước 2 → thiếu 15,5 phút cuối video
- **Hiện tượng:** scene-breakdown dừng ở 00:32:29 trong khi SRT dài 00:48:01; 4 scene Dur=0; metadata.json viết tay khai tổng 2881s nhưng cộng thực 1949s — lỗi bị che, không ai biết.
- **Gốc rễ:** AI giữ toàn bộ bảng trong đầu rồi viết 1 lần; AI tự tuyên bố "xong" không máy kiểm; metadata viết tay nên số tổng bịa được.
- **Phòng ngừa:** quy tắc segment ~10 phút + append ngay (CLAUDE.md bước 2); gate coverage `export.py --step2`; bước 5 do script sinh, cấm viết tay.

## [2026-07-02] Ảnh lúc người thật lúc tranh vẽ
- **Hiện tượng:** cùng 1 bộ prompt, ảnh render lúc photorealistic lúc illustration.
- **Gốc rễ:** style anchor trộn 2 medium (`cinematic realistic illustration`) — mỗi lần generate model tự chọn 1 hướng.
- **Phòng ngừa:** Rule 2.1a (style dứt khoát 1 medium, 2 preset chuẩn); gate `--step4` chặn style trộn medium; ảnh ref nhân vật phải cùng medium.

## [2026-07-02] Hòm "chứa bí mật" → ảnh vẽ hòm mở
- **Hiện tượng:** narration nói hòm khó mở chứa bí mật, ảnh ra hòm đang mở.
- **Gốc rễ:** prompt mô tả ý nghĩa cốt truyện thay vì trạng thái thị giác; model không hiểu cốt truyện; từ phủ định/âm thanh model không vẽ được.
- **Phòng ngừa:** Rule 4.6 Visual State (trạng thái tường minh: closed/locked; cấm secret/hidden/no/not/without/từ âm thanh); gate `--step4` quét tự động.

## [2026-07-02] Lighting trong style anchor đè mood từng scene
- **Hiện tượng:** scene tense (tông lạnh) vẫn ám vàng ấm.
- **Gốc rễ:** style anchor chứa `warm golden lighting` được append vào mọi prompt, đánh nhau với mood keywords.
- **Phòng ngừa:** Rule 2.1a bổ sung — style anchor KHÔNG chứa lighting; lighting chỉ nằm ở Phần 4 (mood_map).

## [2026-07-02] Ảnh không đồng bộ ống kính + cắt cụt đầu ở close-up
- **Hiện tượng:** cùng loại shot nhưng ảnh khác chất; close-up hay bị crop mất đỉnh đầu.
- **Gốc rễ:** prompt thiếu tên lens; thiếu chỉ dẫn an toàn khung hình.
- **Phòng ngừa:** Rule 4.8 (bảng lens cố định theo shot type, `full head within frame` cho close-up); gate `--step4` warn shot thiếu lens.

## [2026-07-03] Video hook phẳng — 3 clip cùng scene gần giống nhau
- **Hiện tượng:** trong 1 scene đấu giá, clip 1/2/3 đều là "Jonah ngồi trong phòng" chỉ khác camera motion — không lột tả diễn biến narration, không tạo cảm xúc, không giữ chân người xem.
- **Gốc rễ:** quy tắc chia clip cũ chia ĐỀU theo thời gian, không theo beat nội dung; video prompt viết từ Description chung của scene thay vì đọc SRT tại từng khoảng giây; thiếu ngôn ngữ dựng phim (escalation wide→medium→close-up, reaction shot).
- **Phòng ngừa:** Rule 6.2 sửa (chia clip theo beat narration, cho phép ảnh phụ `[stt]-[k].jpg` khi cần cú cắt) + Rule 6.7 (mỗi clip 1 beat thị giác mới, escalation, reaction shot); gate `--step4` chặn 2 clip cùng scene trùng lặp ≥75% từ vựng, cảnh báo ≥55%.

## [2026-07-03] Nhân vật biến mất rồi hiện lại giữa video clip
- **Hiện tượng:** video Veo tạo ra ok nhưng người đột ngột biến mất rồi hiện lên lại.
- **Gốc rễ:** video prompt chứa từ đứt gãy thời gian ("revealing" — Clip 1.3); subject không được neo bằng nano_name + đặc điểm nhận diện; camera/hành động không giữ subject trong khung liên tục.
- **Phòng ngừa:** Rule 6.6 Subject Persistence; gate `--step4` chặn từ đứt gãy (suddenly/appears/disappears/reveals/transforms...) + chặn video prompt thiếu nano_name; QC checklist mục Subject Persistence.

## [2026-07-05] Nhân vật đổi màu trang phục giữa các scene khi generate footage
- **Hiện tượng:** cùng 1 nhân vật (vd Clare) mặc áo trắng ở scene này, áo đỏ ở scene liền kề — không nhất quán khi Flow generate ảnh/clip.
- **Gốc rễ:** character description trong `config.json` chỉ ghi loại trang phục ("sharp tailored blazer") mà không ghi MÀU cụ thể — anchor không khóa đủ nên mỗi lần generate model tự chọn màu ngẫu nhiên khác nhau. 7/8 nhân vật của project này mắc lỗi (chỉ Owen bản lao công có màu sẵn).
- **Phòng ngừa:** Rule 1.1 bổ sung — mọi trang phục trong character description PHẢI kèm màu cụ thể, không chỉ tên loại trang phục; đã vá lại config.json + character-refs.md + prompts.md + video-prompts.md của project hiện tại. Gate tự động cho việc này: CHƯA CÓ — cần bổ sung (khó quét máy vì "màu thiếu" đòi hỏi hiểu ngữ nghĩa danh từ trang phục, không đơn thuần word-match).

## [2026-07-06] Uni-X export không gắn được asset bối cảnh vì prompt dùng cụm rút gọn khác keywords config
- **Hiện tượng:** `--unix-export` cảnh báo 130/178 scene "không xác định được setting" — Assets chỉ có nhân vật, thiếu tag bối cảnh (FARM_KITCHEN...).
- **Gốc rễ:** `find_setting_id` khớp bằng cụm ĐẦU TIÊN (trước dấu phẩy) của `settings[].keywords`; ở Bước 4 các batch dùng cụm bối cảnh chuẩn hóa RÚT GỌN (cho vừa ngân sách 75 từ) khác với cụm đầu trong config → không khớp chuỗi.
- **Phòng ngừa:** quy ước mới — cụm đầu tiên của `settings[].keywords` CHÍNH LÀ cụm bối cảnh dùng nguyên văn trong prompt (một nguồn duy nhất, Bước 4 copy từ đó); đã căn lại 11 setting trong config project này cho khớp 178 prompt đã sinh. Gate: `--unix-export` sẵn có cảnh báo liệt kê scene không khớp — sau khi căn, 30 scene còn lại là cận cảnh chi tiết/không gian phụ không có setting entry (chấp nhận, không cần asset bối cảnh cho close-up).

## [2026-07-06] QC3 coi mọi character ID có "_" là variant quá khứ → đòi flashback_style sai
- **Hiện tượng:** project Harlan Boone có các variant tiến triển theo timeline chính (`eli_9`, `eli_12`, `kessler_1978`, `kessler_sunday`, `eli_pajamas`) — QC3 sẽ cảnh báo "thiếu flashback_style" cho ~25 scene dù các scene này KHÔNG phải hồi tưởng; trong khi nhân vật flashback thật (`annie`, không có "_") lại không được QC3 kiểm.
- **Gốc rễ:** gate QC3 dùng heuristic `"_" in id` để đoán "variant quá khứ" — đúng cái bẫy đã ghi ở CLAUDE.md (không suy đoán variant qua quy ước đặt tên ID). Naming format Rule 1.2 (`[id]_[context]`) dùng "_" cho MỌI loại variant, không riêng flashback.
- **Phòng ngừa:** config thêm field tường minh `flashback_variants` (danh sách character ID mà mọi scene chứa nó phải kèm flashback_style); `export.py --qc` ưu tiên field này, chỉ fallback heuristic "_" khi config không khai báo. Project hiện tại: `"flashback_variants": ["annie"]`.

## [2026-07-05] Dùng "Name's" (sở hữu cách dính liền tên) làm hỏng tham chiếu ảnh nhân vật
- **Hiện tượng:** prompt viết dạng "Dalton's blank puzzled expression..." — gắn 's ngay sau nano_name. User báo đây là lỗi nghiêm trọng: Flow tham chiếu ảnh ingredient bằng cách khớp đúng chuỗi nano_name, ký tự dính liền ngay sau tên (như 's) phá vỡ khớp lệnh tham chiếu.
- **Gốc rễ:** Rule 1.1 (Character Lock) trước đây chỉ yêu cầu nano_name "xuất hiện" trong prompt mà không quy định RÕ nano_name phải xuất hiện ở dạng nguyên vẹn (không gắn liền ký tự khác ngay sau). Hệ quả phụ: gate QC1 hiện tại kiểm bằng substring (`nn.lower() in body`) nên "dalton's" vẫn được tính là "có nano_name" — không bắt được lỗi này, gây lọt lưới.
- **Phòng ngừa:** Rule 1.1 bổ sung mục cấm sở hữu cách dính liền nano_name + hướng dẫn diễn đạt lại; gate `--step4`/`--qc` thêm check riêng quét pattern `{nano_name}'s` (word boundary) — LỖI cứng, không phải cảnh báo, vì đây là hỏng chức năng tham chiếu thật chứ không phải vấn đề văn phong.

## [2026-07-11] 2 clip hook liền kề cùng camera motion — lọt gate similarity
- **Hiện tượng:** hook clip 2.1 (wide 3 người cười, push-in) và 2.2 (ECU mặt Coy, push-in) — hai ảnh/khung KHÁC hẳn nhau nhưng CÙNG chuyển động máy "push-in". Gate `--step4` PASS vì similarity từ vựng thấp (chủ thể + scale khác); chỉ QC định tính (checklist HOOK) bắt được.
- **Gốc rễ:** Rule 6.7 chỉ tự động hóa bằng similarity TỪ VỰNG giữa 2 clip cùng scene (≥75% lỗi, ≥55% cảnh báo). Camera motion không nằm trong phép đo đó, và check chỉ so trong cùng scene — không so 2 clip liền kề khác scene. Checklist QC có ghi "không 2 clip liền kề cùng camera motion" nhưng chưa có gate máy.
- **Phòng ngừa:** `export.py > check_video_hook` thêm pass mới — trích primary camera-motion token của mọi clip theo thứ tự tài liệu (push-in/pull-back/tracking/pan/static camera/camera holds/dolly/tilt/zoom/sway...), 2 clip LIỀN KỀ (kể cả khác scene) cùng token → CẢNH BÁO (Rule 6.7). Rule 6.7 (video-prompt.md) làm rõ camera motion cũng phải đổi giữa các cú cắt. Đã vá clip 2.2 project hiện tại sang static hold.

## [2026-07-12] "without mustache" trong character description làm lọt từ phủ định vào prompt + lệch anchor giữa các batch
- **Hiện tượng:** project Amish (bắt buộc râu quai nón không ria kiểu Amish), 3 nhân vật có `description` ghi `chin-curtain beard without mustache`. Khi Bước 4 chạy sub-agent song song, mỗi agent xử lý cụm `without` (từ phủ định Rule 4.6) một kiểu khác nhau: batch giữ nguyên (dính cảnh báo negation), batch đổi `dark chin-curtain beard`, batch đổi `with clean-shaven upper lip` → cùng 1 nhân vật mô tả râu khác chữ giữa các scene.
- **Gốc rễ:** `description` viết theo lối phủ định (`without mustache`) thay vì positive framing. Rule 4.6 cấm `no/not/without/never` trong prompt; anchor kéo từ description nên "without" lan vào prompt, và vì gate chỉ cảnh báo (không chặn) nên mỗi agent tự diễn đạt lại → mất nhất quán chữ nghĩa của anchor.
- **Phòng ngừa:** quy ước — mọi `characters[].description` (Bước 1) PHẢI dùng positive framing, KHÔNG chứa `no/not/without/never`; diễn đạt "không ria" thành `clean-shaven upper lip`. Đã vá config.json + character-refs.md project hiện tại (gideon/ezra/yost). Gate tự động: `--step2` có thể bổ sung quét `\b(no|not|without|never)\b` trong `characters[].description` và cảnh báo — CHƯA CÓ, cần bổ sung. Prompts đã sinh giữ anchor lõi `chin-curtain beard` nhất quán nên không cần regenerate.

## [2026-07-12] Sub-agent Bước 4 tạo prompt thiếu closeup/angle theo thập kỷ → phải rebalance cả loạt ở QC
- **Hiện tượng:** QC5b cảnh báo ~27 scene: 10/15 thập kỷ thiếu nhóm `closeup` (0-1/2), 8/15 thiếu `angle` (0/1); kèm vài chuỗi 4 shot `medium` liên tiếp. Prompt dồn về wide/medium.
- **Gốc rễ:** prompt giao cho 6 sub-agent Bước 4 chỉ nói "VARY shots" mà KHÔNG ép rõ mỗi thập kỷ tuyệt đối [1-10],[11-20]... phải đạt min Rule 4.2 (≥2 wide, ≥3 medium, ≥2 closeup, ≥1 angle). Thêm nữa `shot_cat` phân loại theo LENS: "medium close-up 85mm lens" bị tính là `medium` (không phải closeup) → agent tưởng đã có closeup nhưng máy đếm 0. Kết quả phải chạy 1 lượt rebalance ~30 prompt (đổi Part 3 sang `close-up 85mm portrait lens`/`extreme close-up 100mm macro lens`/`over-shoulder|low|high angle shot`) ở Bước 7.
- **Phòng ngừa:** prompt sub-agent Bước 4 PHẢI liệt kê min Rule 4.2 theo thập kỷ tuyệt đối + nêu rõ token lens quyết định nhóm (closeup ⟺ `85mm portrait`/`100mm macro`; angle ⟺ over-shoulder/low/high angle; wide ⟺ `24mm`/`35mm lens`; "medium close-up 85mm lens" = MEDIUM). Với emotional_peak ưu tiên close-up mặt (+`full head within frame`); đối đầu/quyền lực ưu tiên angle. Gate: `--qc` QC5b sẵn có cảnh báo — nhưng nên bắt sớm ở Bước 4 thay vì rebalance cả loạt ở Bước 7. Đã vá project hiện tại: rebalance đạt Rule 4.2 mọi thập kỷ, 0 cảnh báo shot.
